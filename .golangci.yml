run:
  timeout: 5m

linters:
  enable:
    - errcheck      # Unchecked errors
    - govet         # Go vet
    - ineffassign   # Unused assignments
    - staticcheck   # Static checker
    - unused        # Unused code
