telegram:
  token: "${TG_TOKEN}"
  admin_id: ${TG_ADMIN_ID}
  timeout: 10

hiddify:
  base_url: "${HIDDIFY_BASE_URL}"
  admin_proxy: "${HIDDIFY_ADMIN_PROXY}"
  api_key: "${HIDDIFY_API_KEY}"

db:
  path: "${DB_PATH:-data/bot.db}"

log:
  level: "${LOG_LEVEL:-info}"
